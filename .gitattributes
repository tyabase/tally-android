# 换行符策略 —— 这个文件不是可选的美化项。
#
# 本机 core.autocrlf=true，如果不显式约定，git 会在 checkout 时把 LF 换成 CRLF。
# 首当其冲的是 gradlew：CRLF 的 shell 脚本在 Linux / macOS / CI 上会直接报
#     /usr/bin/env: 'sh\r': No such file or directory
# 也就是「本地好好的，一进 CI 就挂」。仓库内统一 LF，Windows 用户靠编辑器设置处理即可。

# 默认：仓库内一律 LF
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则 cmd.exe 解析会出问题
*.bat text eol=crlf
*.cmd text eol=crlf

# Gradle wrapper 的 shell 脚本必须保持 LF（显式声明，防被上面的规则或本机配置覆盖）
gradlew text eol=lf

# 二进制：不要做任何换行符转换，也不要尝试 diff
*.jar  binary
*.jks  binary
*.keystore binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.ico  binary
*.apk  binary
*.aab  binary
*.zip  binary
*.ttf  binary
*.otf  binary

# 让 GitHub 语言统计别把生成物算进去
*.mjs linguist-language=JavaScript
