# ── 签名密钥：绝对不要提交 ──────────────────────────────────
# 密钥一旦进入提交历史就再也删不干净，任何人都能以你的名义签名 APK。
keystore.properties
*.jks
*.keystore

# ── 本机路径 ────────────────────────────────────────────────
# sdk.dir / ndk.dir 写的是本机绝对路径，别人 clone 后会直接构建失败。
local.properties

# ── 构建产物 ────────────────────────────────────────────────
build/
.gradle/
.cxx/
captures/
*.apk
*.aab
*.ap_
*.dex
*.class

# ── 编辑器 / IDE ────────────────────────────────────────────
.idea/
*.iml
*.iws
*.ipr
.vscode/
.DS_Store
Thumbs.db

# ── 日志与临时文件 ──────────────────────────────────────────
*.log
*.hprof
.kotlin/
